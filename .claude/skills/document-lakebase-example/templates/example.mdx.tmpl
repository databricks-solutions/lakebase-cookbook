---
title: __TITLE__
sidebar_label: __SIDEBAR_LABEL__
category: __CATEGORY__
sidebar_position: __SIDEBAR_POSITION__
description: __DESCRIPTION__
---
import Callout from '../../../components/Callout.astro';

# __TITLE__

Short, styled summary of what the example does and which Lakebase capability it
demonstrates. Keep this tighter than the example's own README — summarize and
link, don't duplicate.

## Architecture

```
(ASCII diagram — who calls what, and where Lakebase sits)
```

A sentence or two on the data flow.

## Deploy with Asset Bundles

Prerequisites: <Apps / Genie space / SQL warehouse / Lakebase project with the
required extension / ...>.

```bash
databricks bundle deploy -t demo \
  --var lakebase_database="projects/<project>/branches/<branch>/databases/<id>"
```

Add any once-per-workspace setup steps.

## Configuration

| Variable / setting | Purpose |
|--------------------|---------|
| `lakebase_database` | Full Lakebase database resource path (required). |
| ... | ... |

<Callout type="note" title="Source">
The full example lives in
[`__NAME__/`](https://github.com/databricks-solutions/lakebase-cookbook/tree/main/__NAME__).
</Callout>
