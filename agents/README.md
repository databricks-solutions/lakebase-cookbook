# Agents

Examples for building AI agents on Lakebase — durable memory and
knowledge-graph retrieval on managed Postgres.

| Example | Description |
|---------|-------------|
| [`graphrag/`](graphrag/) | Knowledge-graph-augmented RAG served from Lakebase: a pgvector semantic seed plus recursive-CTE graph traversal over `nodes`/`edges` tables. |
| [`ai_memory/`](ai_memory/) | AI memory patterns backed by Lakebase Postgres — short- and long-term memory for agents. |
